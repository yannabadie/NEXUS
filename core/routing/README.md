# routing

NEXUS V7 Model Routing Module

Routes tasks to appropriate models based on complexity and task type.
Supports Opus/Sonnet selection for Claude and model variants for Gemini.

## Overview

| Metric | Value |
|--------|-------|
| **Path** | `C:\Code\NEXUS\NEXUS-N7A\core\routing` |
| **Modules** | 2 |
| **Total Lines** | 346 |
| **Classes** | 3 |
| **Functions** | 0 |

## Architecture

```mermaid
classDiagram
    class TaskType {
        +BRAINSTORM
        +REDTEAM
        +ARCHITECT
        +EVOLUTION
        +REASONING
        +RESEARCH
        +ANALYSIS
        +TOOL
        +VALIDATION
        +SIMPLE
        +FORMAT
        +DEFAULT
    }
    Enum <|-- TaskType
    class RoutingDecision {
        +str model_id
        +TaskType task_type
        +str reason
        +bool is_opus
    }
    class ModelRouter {
        +opus_model
        +sonnet_model
        +gemini_model
        +gemini_pro_model
        +gemini_flash_model
        +opus_tasks
        +sonnet_tasks
        +gemini_pro_tasks
        +gemini_flash_tasks
        -__init__(self, config: Optional['Config']=...)
        +select_claude_model(self, task_type: TaskType) str
        +select_claude_model_str(self, task_type_str: str) str
        +route(self, task_type: TaskType) RoutingDecision
        +get_gemini_model(self) str
        +select_gemini_model(self, task_type: TaskType) str
        +select_gemini_model_str(self, task_type_str: str) str
        +should_use_gemini_pro(self, task_type: TaskType) bool
        +should_use_opus(self, task_type: TaskType) bool
        +select_best_agent(self, task_type: TaskType, agent_pool: Optional['AgentPool']=..., min_importance: float=...) RoutingDecision
        +get_routing_stats(self, agent_pool: Optional['AgentPool']=...) dict
    }
```

## Modules

| Module | Description | Classes | Functions |
|--------|-------------|---------|-----------|
| [model_router](model_router.py) | Model Router - Intelligent Model Selection for NEXUS V7 Chrysalis | 3 | 0 |





---
*Auto-generated by nexus-doc-generator 1.0.0 - 2025-12-16 19:13*