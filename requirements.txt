# NEXUS V8.0 TRUE HIVE MIND Dependencies
# Philosophy: Minimal dependencies, maximum value

# =============================================================================
# Core Dependencies (REQUIRED)
# =============================================================================

# Configuration management
python-dotenv>=1.0.0

# Data validation and settings
pydantic>=2.0.0

# Rich console output (tables, progress, syntax highlighting)
rich>=13.7.0

# Interactive REPL with history and completion
prompt-toolkit>=3.0.43

# Token counting for budget tracking
tiktoken>=0.5.2

# =============================================================================
# Phase 10e: BM25S Sparse Retrieval (Project Memory RAG)
# =============================================================================
# BM25S: 500x faster than rank-bm25, pure Python + Scipy sparse matrices
# Used for: ProjectMemory.retrieve() - better recall than TF-IDF (+15%)
bm25s>=0.2.0

# PyStemmer: Snowball stemming for better term matching
# Optional but recommended for +5% recall improvement
PyStemmer>=2.2.0

# =============================================================================
# Phase 10g: Dense Embeddings (Semantic Retrieval) [OPTIONAL]
# =============================================================================
# LanceDB: Embedded vector database, serverless, hybrid search support
# Used for: DenseBackend - semantic similarity search
lancedb>=0.4.0

# Sentence-Transformers: State-of-the-art text embeddings
# Model: all-MiniLM-L6-v2 (22MB, 384 dim, ~5k sent/sec CPU)
# Provides ~+10% recall vs BM25S for semantic queries
# V10 MEMORY FORGE: Updated for ONNX backend support (2-3x faster CPU)
sentence-transformers>=3.2.0

# ONNX Runtime: Optimized inference for embeddings
# V10 MEMORY FORGE: CPU acceleration for embedding model
# Install with: pip install sentence-transformers[onnx] for full support
onnxruntime>=1.19.0

# =============================================================================
# V10 SaaS: Multi-Tenant Data Layer (OPERATION PRISM)
# =============================================================================
# SQLModel: Pydantic + SQLAlchemy integration for type-safe ORM
sqlmodel>=0.0.14

# SQLAlchemy: Database toolkit (required by SQLModel)
sqlalchemy>=2.0

# =============================================================================
# V10 CEREBRO: Redis Event Bus & API (OPERATION CEREBRO: GENESIS)
# =============================================================================
# Redis: Async pub/sub for external UI observation
redis>=5.0.0

# FastAPI: High-performance ASGI framework for WebSocket API
fastapi>=0.115.0

# Uvicorn: ASGI server with WebSocket support
uvicorn[standard]>=0.32.0

# WebSockets: Low-level WebSocket implementation
websockets>=13.0

# Python-Jose: JWT encoding/decoding for API auth
python-jose[cryptography]>=3.3.0

# =============================================================================
# Development Dependencies (REQUIRED for tests)
# =============================================================================
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# =============================================================================
# Notes
# =============================================================================
# Standard library modules used (no install needed):
# - json, pathlib, re, enum, dataclasses, typing
# - threading, subprocess, hashlib, uuid
# - datetime, logging, functools
#
# System tools required (install separately):
# - gemini CLI (Google AI)
# - claude CLI (Anthropic)
