flowchart {{ direction }}
{% for node in nodes %}
    {{ node.id }}{% if node.shape == 'round' %}({{ node.label }}){% elif node.shape == 'diamond' %}{{"{"}}{{ node.label }}{{"}"}}{% elif node.shape == 'stadium' %}([{{ node.label }}]){% elif node.shape == 'subroutine' %}[[{{ node.label }}]]{% else %}[{{ node.label }}]{% endif %}

{% endfor %}

{% for edge in edges %}
    {{ edge.from }} {% if edge.style == 'dotted' %}-.->{% elif edge.style == 'thick' %}==>{% else %}-->{% endif %}{% if edge.label %}|{{ edge.label }}|{% endif %} {{ edge.to }}
{% endfor %}

{% for subgraph in subgraphs %}
    subgraph {{ subgraph.id }}[{{ subgraph.label }}]
{% for node_id in subgraph.nodes %}
        {{ node_id }}
{% endfor %}
    end
{% endfor %}
