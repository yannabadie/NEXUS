# session

NEXUS Session Management Module.

V9.7.1 - Context Bleeding Fix (Improved)

This module provides session isolation for parallel task execution
without context bleeding between agents.

Key Components:
- SessionWorkspaceManager: Isolated workspaces per session/task
- HomeIsolator: HOME environment spoofing for Gemini session isolation (V9.7.1)

V9.7.1 HOME Spoofing (replaces V9.7 CWD Isolation):
- V9.7 CWD Isolation caused "ghost files" (writes to wrong directory)
- V9.7.1 uses HOME spoofing: CWD stays at project root, HOME is isolated
- Gemini CLI stores sessions in ~/.gemini/tmp/<hash(cwd)>/chats/
- Different HOME = Different session storage = Isolation without ghost files

Usage:
    from core.session import SessionWorkspaceManager, HomeIsolator

    # Create managers
    manager = SessionWorkspaceManager(workspace_path)

    # Get isolated environment for Gemini subprocess (V9.7.1)
    isolated_env = manager.get_isolated_env("task_123_lead", "swarm")

    # Pass to subprocess (CWD stays at project root!)
    subprocess.Popen(cmd, env=isolated_env, cwd=workspace_path)

    # Cleanup after task completion
    manager.cleanup_workspace("task_123_lead", "swarm")

## Overview

| Metric | Value |
|--------|-------|
| **Path** | `C:\Code\NEXUS\NEXUS-N7A\core\session` |
| **Modules** | 3 |
| **Total Lines** | 969 |
| **Classes** | 2 |
| **Functions** | 2 |

## Architecture

```mermaid
classDiagram
    class HomeIsolator {
        -_SESSION_ID_PATTERN
        +base_path
        +homes_dir
        -_use_workspace_prefix
        -_lock
        -__init__(self, base_path: Path, use_workspace_prefix: bool=...)
        -_sanitize_session_id(self, session_id: str) str
        +get_isolated_env(self, session_id: str) Dict[str, str]
        +get_home_path(self, session_id: str) Optional[Path]
        +acquire_env(self, session_id: str) Dict[str, str]
        +release_env(self, session_id: str) int
        +get_ref_count(self, session_id: str) int
        +cleanup_home(self, session_id: str, force: bool=...) bool
        +cleanup_old_homes(self, max_age_hours: float=...) int
        +list_active_homes(self) list[str]
        +get_stats(self) Dict[str, any]
        +check_disk_quota(self, quota_mb: float=..., warn_threshold: float=...) Dict[str, any]
        +enforce_quota(self, quota_mb: float=...) int
    }
    class SessionWorkspaceManager {
        +base_workspace
        +sessions_dir
        -_lock
        -_home_isolator
        -__init__(self, base_workspace: Path) None
        +get_or_create_workspace(self, session_id: str, session_type: str=...) Path
        -_setup_workspace(self, workspace_path: Path) None
        +get_workspace(self, session_id: str, session_type: str=...) Optional[Path]
        +cleanup_workspace(self, session_id: str, session_type: str=...) bool
        +cleanup_old_workspaces(self, max_age_hours: int=...) int
        +get_default_workspace(self) Path
        +get_isolated_env(self, session_id: str, session_type: str=...) Dict[str, str]
        +cleanup_isolated_env(self, session_id: str, session_type: str=...) bool
        +list_active_workspaces(self) Dict[str, Path]
        +get_stats(self) Dict[str, int]
        -__repr__(self) str
    }
```

## Modules

| Module | Description | Classes | Functions |
|--------|-------------|---------|-----------|
| [home_isolator](home_isolator.py) | HomeIsolator - Session Isolation via HOME Environment Spoofing. | 1 | 0 |
| [workspace_manager](workspace_manager.py) | SessionWorkspaceManager - Isolated Workspaces for Session Isolation. | 1 | 2 |





---
*Auto-generated by nexus-doc-generator 1.0.0 - 2025-12-16 19:13*