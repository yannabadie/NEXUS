# workflow

NEXUS V12.3 SCALE-OUT - Workflow Module

Redis-backed workflow registry with graceful degradation to in-memory.
Enables multi-instance deployment where workflows are visible across all nodes.

Components:
- RedisWorkflowRegistry: Redis-backed workflow storage
- DistributedLock: Redlock pattern for concurrent safety
- get_workflow_registry(): Factory function with graceful degradation

Author: Claude (NEXUS V12.3 SCALE-OUT)
Date: 2025-12-16

## Overview

| Metric | Value |
|--------|-------|
| **Path** | `C:\Code\NEXUS\NEXUS-N7A\core\workflow` |
| **Modules** | 3 |
| **Total Lines** | 826 |
| **Classes** | 4 |
| **Functions** | 4 |

## Architecture

```mermaid
classDiagram
    class DistributedLock {
        +KEY_PREFIX
        +DEFAULT_TTL
        -_redis
        -_resource
        -_ttl
        -_owner_id
        -_acquired
        -__init__(self, redis: Optional[Any], resource: str, ttl: int=..., owner_id: Optional[str]=...)
        +key(self) str
        +is_acquired(self) bool
        +acquire(self, blocking: bool=..., timeout: float=...) bool
        +release(self) bool
        +extend(self, additional_ttl: int=...) bool
        -__aenter__(self) 'DistributedLock'
        -__aexit__(self, exc_type, exc_val, exc_tb) None
    }
    class LockAcquisitionError {
    }
    Exception <|-- LockAcquisitionError
    class WorkflowStatus {
        +PENDING
        +RUNNING
        +COMPLETED
        +FAILED
        +CANCELLED
    }
    str <|-- WorkflowStatus
    Enum <|-- WorkflowStatus
    class RedisWorkflowRegistry {
        -Optional['RedisWorkflowRegistry'] _instance
        -_lock
        +WORKFLOW_TTL
        +KEY_PREFIX
        -_initialized
        -__new__(cls) 'RedisWorkflowRegistry'
        -__init__(self)
        +configure(self, redis_url: str=..., use_redis: bool=..., workflow_ttl_hours: int=...) None
        +connect(self) bool
        +disconnect(self) None
        -_workflow_key(self, tenant_id: str) str
        -_serialize(self, data: Dict[str, Any]) str
        -_deserialize(self, data: str) Dict[str, Any]
        +create_workflow(self, workflow_id: str, tenant_id: str, task: str, workspace_id: str=..., complexity: Optional[str]=...) Dict[str, Any]
        -_store_in_memory(self, workflow_id: str, tenant_id: str, workflow: Dict[str, Any]) None
        +update_status(self, workflow_id: str, tenant_id: str, status: str, result: Any=..., error: Optional[str]=...) Optional[Dict[str, Any]]
        -_update_in_memory(self, workflow_id: str, workflow: Dict[str, Any]) None
        +get_workflow(self, workflow_id: str, tenant_id: str) Optional[Dict[str, Any]]
        +list_workflows(self, tenant_id: str, status: Optional[str]=..., limit: int=...) List[Dict[str, Any]]
        +delete_workflow(self, workflow_id: str, tenant_id: str) bool
        +cleanup_expired(self, max_age_hours: int=...) int
        +get_stats(self) Dict[str, Any]
    }
```

## Modules

| Module | Description | Classes | Functions |
|--------|-------------|---------|-----------|
| [distributed_lock](distributed_lock.py) | NEXUS V12.3 SCALE-OUT - Distributed Lock | 2 | 2 |
| [redis_registry](redis_registry.py) | NEXUS V12.3 SCALE-OUT - Redis Workflow Registry | 2 | 2 |





---
*Auto-generated by nexus-doc-generator 1.0.0 - 2025-12-16 19:13*