# analyzer

Code analysis using AST parsing.

Classes:
    PythonAnalyzer: Analyzes Python files using ast module
    TypeScriptAnalyzer: Analyzes TypeScript files
    DependencyGraphBuilder: Builds import dependency graph
    MetricsExtractor: Extracts code metrics (LOC, complexity)

## Overview

| Metric | Value |
|--------|-------|
| **Path** | `C:\Code\NEXUS\NEXUS-N7A\tools\doc_generator\analyzer` |
| **Modules** | 3 |
| **Total Lines** | 604 |
| **Classes** | 3 |
| **Functions** | 0 |

## Architecture

```mermaid
classDiagram
    class SimpleGraph {
        -_nodes
        -_edges
        -__init__(self)
        +add_node(self, node: str, **attrs)
        +add_edge(self, from_node: str, to_node: str, **attrs)
        +number_of_nodes(self) int
        +number_of_edges(self) int
        +nodes(self, data: bool=...)
        +edges(self, data: bool=...)
        +in_edges(self, node: str)
        +out_edges(self, node: str)
        +predecessors(self, node: str)
        +successors(self, node: str)
        +has_node(self, node: str) bool
        +get_node_data(self, node: str) dict
    }
    class DependencyGraphBuilder {
        +config
        +repo_root
        -__init__(self, config: DocGeneratorConfig)
        +build(self, modules: list[ModuleInfo]) 'nx.DiGraph | SimpleGraph'
        -_path_to_module_name(self, path: Path) str
        -_resolve_import(self, import_name: str, module_paths: dict[str, ModuleInfo]) Optional[str]
        +get_internal_dependencies(self, module_name: str, graph) list[str]
        +get_dependents(self, module_name: str, graph) list[str]
        +get_external_dependencies(self, modules: list[ModuleInfo]) set[str]
        -_is_internal_import(self, module_name: str) bool
        +to_mermaid(self, graph, max_nodes: int=...) str
        +get_clusters(self, graph) dict[str, list[str]]
        +calculate_metrics(self, graph) dict
    }
    class PythonAnalyzer {
        +config
        +repo_root
        -__init__(self, config: DocGeneratorConfig)
        +analyze_file(self, file_path: Path) ModuleInfo
        -_extract_classes(self, tree: ast.AST) list[ClassInfo]
        -_analyze_class(self, node: ast.ClassDef) ClassInfo
        -_extract_functions(self, tree: ast.AST) list[FunctionInfo]
        -_analyze_function(self, node: ast.FunctionDef | ast.AsyncFunctionDef) FunctionInfo
        -_build_signature(self, node: ast.FunctionDef | ast.AsyncFunctionDef) str
        -_format_arg(self, arg: ast.arg) str
        -_get_annotation_name(self, node: ast.expr) str
        -_get_attribute_name(self, node: ast.Attribute) str
        -_get_decorator_name(self, node: ast.expr) str
        -_extract_class_attributes(self, node: ast.ClassDef) list[dict]
        -_extract_imports(self, tree: ast.AST) list[ImportInfo]
        +get_public_api(self, module: ModuleInfo) dict
        +get_docstring_summary(self, docstring: Optional[str]) str
    }
```

## Modules

| Module | Description | Classes | Functions |
|--------|-------------|---------|-----------|
| [dependency_graph](dependency_graph.py) | Builds import dependency graphs using networkx. | 2 | 0 |
| [python_analyzer](python_analyzer.py) | Analyzes Python files using the ast module. | 1 | 0 |





---
*Auto-generated by nexus-doc-generator 1.0.0 - 2025-12-16 19:13*