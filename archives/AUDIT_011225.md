 Plan de Remédiation NEXUS V7 - Communication Gemini↔Claude
Action: Diagnostic complet + Plan de remédiation
Statut: ✅ ANALYSE TERMINÉE
🔴 PROBLÈMES CRITIQUES IDENTIFIÉS
BUG #1: ALTERNANCE NON FORCÉE (BRAINSTORMING)
Fichier: orchestration_v7.py:636-639

# CODE ACTUEL (CASSÉ)
next_agent = message.get("next_agent", self.active_agent)  # ⚠️ Default = reste sur le même
if next_agent != self.active_agent:
    self.active_agent = next_agent

Problème: L'orchestrateur dépend de next_agent dans la réponse de l'agent. Si l'agent ne renvoie pas next_agent, l'alternance ne se fait PAS!

Preuve: Le mode EVOLUTION_BRAINSTORM (lignes 796-798) FORCE l'alternance:

# FONCTIONNE CORRECTEMENT
self.active_agent = "Claude" if self.active_agent == "Gemini" else "Gemini"

BUG #2: CLAUDE DRIVER GARDE "CLAUDE" PAR DÉFAUT
Fichier: claude_driver_hybrid.py:298-301

# CODE ACTUEL (CASSÉ)
next_agent = "Claude"  # ⚠️ Default = reste sur Claude!
if "gemini" in content.lower() and action_type == "TALK":
    next_agent = "Gemini"  # Seulement si "gemini" est mentionné

Problème: Claude ne délègue à Gemini que si le mot "gemini" apparaît dans sa réponse!

BUG #3: MODE SWARM - ERREURS CLAUDE SILENCIEUSES
Fichier: mode_executors.py:129-167

Les executors détectent les erreurs mais ne les montrent pas clairement à l'utilisateur. Si Claude timeout, l'output n'est pas formaté correctement.

✅ PLAN DE REMÉDIATION DÉTAILLÉ
CORRECTION #1: Forcer l'alternance dans BRAINSTORMING
Fichier: orchestration_v7.py Lignes: 636-641

# AVANT (CASSÉ):
next_agent = message.get("next_agent", self.active_agent)
if next_agent != self.active_agent:
    self.active_agent = next_agent
    self.stagnation_detector.reset()

# APRÈS (CORRIGÉ):
# V7 FIX: FORCE alternance égale Gemini↔Claude (comme EVOLUTION_BRAINSTORM)
# L'alternance ne doit PAS dépendre de next_agent de l'agent
previous_agent = self.active_agent
self.active_agent = "Claude" if self.active_agent == "Gemini" else "Gemini"
self.stagnation_detector.reset()

# Log l'alternance
if self.config.ui_verbose:
    print(f"[FSM] Agent switch: {previous_agent} → {self.active_agent}")

CORRECTION #2: Claude driver - Alterner par défaut
Fichier: claude_driver_hybrid.py Lignes: 298-301

# AVANT (CASSÉ):
next_agent = "Claude"  # Default stay with Claude
if "gemini" in content.lower() and action_type == "TALK":
    next_agent = "Gemini"

# APRÈS (CORRIGÉ):
# V7 FIX: Alterner par défaut pour collaboration égale
# L'orchestrateur force déjà l'alternance, mais on aide ici aussi
next_agent = "Gemini"  # Default: passer à Gemini après chaque réponse
if "je continue" in content.lower() or "i'll continue" in content.lower():
    next_agent = "Claude"  # Claude garde la main seulement si explicite

CORRECTION #3: Améliorer feedback visuel SWARM
Fichier: orchestration_v7.py Lignes: 502-520 (dans process_turn, section SWARM output)

# AJOUTER avant formatted_output:
# V7 FIX: Montrer clairement qui parle et détecter les erreurs
for agent_data in agent_outputs:
    agent_id = agent_data.get("agent_id", "")
    content = agent_data.get("content", "")
    status = agent_data.get("status", "success")
    agent_name = "Gemini" if "gemini" in agent_id.lower() else "Claude"
    
    if status == "error" or content.startswith("Error:"):
        formatted_output += f"\n🔴 {agent_name} ERROR:\n{content}\n---\n"
    else:
        formatted_output += f"\n🟢 {agent_name}:\n{content}\n---\n"

CORRECTION #4: Ajouter logs de debug pour tracer le flux
Fichier: orchestration_v7.py Ajouter après ligne 583 (dans BRAINSTORMING, après response = self._invoke_agent()):

# V7 DEBUG: Tracer les invocations d'agents
self.logger.debug("Agent invoked", {
    "agent": self.active_agent,
    "response_preview": str(response)[:200],
    "action_type": response.get("action_type"),
    "next_agent": response.get("next_agent")
})

📋 RÉSUMÉ DES FICHIERS À MODIFIER
Fichier	Lignes	Correction
orchestration_v7.py	636-641	Forcer alternance
orchestration_v7.py	502-520	Améliorer feedback SWARM
orchestration_v7.py	~583	Ajouter logs debug
claude_driver_hybrid.py	298-301	Alterner par défaut
⚡ PRIORITÉ D'EXÉCUTION
CRITIQUE - Correction #1 (alternance forcée) - C'est LE bug principal
HAUTE - Correction #2 (Claude driver) - Support additionnel
MOYENNE - Correction #3 (feedback visuel) - UX
BASSE - Correction #4 (logs debug) - Diagnostic
Update Todos
 Analyser le flux FSM orchestration
 Tracer la communication Gemini ↔ Claude
 Identifier les points de rupture
 Produire plan de remédiation
Driver, le diagnostic est complet.

Le bug principal est clair: l'alternance Gemini↔Claude n'est pas forcée dans le mode BRAINSTORMING normal (contrairement au mode EVOLUTION_BRAINSTORM où ça fonctionne).

Voulez-vous que j'applique les corrections maintenant ? Je peux:

Appliquer toutes les corrections (4 fichiers)
Appliquer seulement la correction critique (#1)
Créer un patch file pour review
En attente de vos instructions.

Prends le temps de crise encore plus pour voir

Compris. Je creuse plus profondément pour trouver TOUS les problèmes.

Update Todos
 Analysant la configuration complète
 Examiner le REPL et le flux utilisateur
 Vérifier les protocoles de message
 Analyser le swarm negotiation
 Identifier TOUS les points de rupture
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/config.py
256 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/interface/repl.py
File content (25956 tokens) exceeds maximum allowed tokens (25000). Please use offset and limit parameters to read specific portions of the file, or use the GrepTool to search for specific content.
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/synapse/protocol_v7.py
128 lignes lues
Update Todos
 Analyser la configuration complète
 Examinant le REPL et le flux utilisateur
 Vérifier les protocoles de message
 Analyser le swarm negotiation
 Identifier TOUS les points de rupture
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/interface/repl.py
200 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/swarm/negotiation_protocol.py
626 lignes lues
Update Todos
 Analyser la configuration complète
 Examiner le REPL et le flux utilisateur
 Vérifier les protocoles de message
 Analyser le swarm negotiation
 Identifiant TOUS les points de rupture
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/orchestration_v7.py
150 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/orchestration_v7.py
100 lignes lues
Excellente découverte ! Maintenant je vois le flux complet. Analysons les invocations SWARM pour Claude.

Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/swarm/mode_executors.py
200 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/orchestration_v7.py
100 lignes lues
Grep
class PingPongExecutor
Grep
get_all_agents|agent_assignments
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/swarm/hybrid_swarm_engine.py
100 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/swarm/hybrid_swarm_engine.py
150 lignes lues
Read
/home/user/NEXUS/NEXUS_V7_CHRYSALIS/core/swarm/mode_selector.py
80 lignes lues
Grep
_create_default_agents|_assign_agents|claude_opus|gemini_primary
Update Todos
 Analyser la configuration complète
 Examiner le REPL et le flux utilisateur
 Vérifier les protocoles de message
 Analyser le swarm negotiation
 Identifier TOUS les points de rupture
🔬 ANALYSE APPROFONDIE COMPLÈTE - RAPPORT FINAL
Vue d'ensemble du flux
┌─────────────────────────────────────────────────────────────────────────────┐
│ USER INPUT                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│ REPL.run() → orchestrator.process_turn(user_input)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────┐                                │
│ │ STATE: IDLE                               │                                │
│ │                                           │                                │
│ │ if swarm_auto_route == True: ←─────────── 🔴 TOUJOURS TRUE PAR DÉFAUT     │
│ │     process_with_swarm()  ←────────────── BYPASS BRAINSTORMING!           │
│ │     if success: return   ←─────────────── PAS D'ALTERNANCE CLASSIQUE      │
│ │                                           │                                │
│ │ else: → BRAINSTORMING                     │                                │
│ └──────────────────────────────────────────┘                                │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────┐                                │
│ │ STATE: BRAINSTORMING (si SWARM échoue)    │                                │
│ │                                           │                                │
│ │ next_agent = message.get("next_agent",    │                                │
│ │              self.active_agent)  ←─────── 🔴 DEFAULT = PAS D'ALTERNANCE   │
│ │                                           │                                │
│ │ protocol_v7.py:                           │                                │
│ │   if next_agent is None:                  │                                │
│ │     return sender  ←───────────────────── 🔴 RENFORCE LE PROBLÈME         │
│ │                                           │                                │
│ │ claude_driver:                            │                                │
│ │   next_agent = "Claude"  ←─────────────── 🔴 JAMAIS "GEMINI"              │
│ └──────────────────────────────────────────┘                                │
└─────────────────────────────────────────────────────────────────────────────┘

🔴 8 POINTS DE RUPTURE IDENTIFIÉS
NIVEAU 1: ARCHITECTURE (cause racine)
#	Fichier	Ligne	Problème	Impact
1	config.py	178	swarm_auto_route=True par défaut	TOUTES les requêtes passent par SWARM
2	orchestration_v7.py	475-541	SWARM capture et retourne avant BRAINSTORMING	Pas d'alternance classique
NIVEAU 2: BRAINSTORMING (si SWARM fallback)
#	Fichier	Ligne	Problème	Impact
3	orchestration_v7.py	636	next_agent = message.get("next_agent", self.active_agent)	Default = reste sur le même agent
4	protocol_v7.py	74-76	Validateur: si next_agent=None → return sender	Renforce le bug #3
5	claude_driver_hybrid.py	298-301	next_agent = "Claude" par défaut	Claude ne délègue jamais à Gemini
NIVEAU 3: SWARM (si utilisé)
#	Fichier	Ligne	Problème	Impact
6	orchestration_v7.py	509-515	Erreurs Claude formatées mais pas clairement visibles	User ne voit pas les erreurs
7	claude_driver_hybrid.py	164-175	Timeout = Exception silencieuse	Pas de feedback utilisateur
8	orchestration_v7.py	246-254	_invoke_for_swarm catch exceptions → return f"Error: {e}"	Erreur avalée
✅ PREUVE QUE L'ALTERNANCE FORCÉE FONCTIONNE
Mode EVOLUTION_BRAINSTORM (lignes 796-798):

# FORCE ALTERNATION in evolution mode - equal participation
previous_agent = self.active_agent
self.active_agent = "Claude" if self.active_agent == "Gemini" else "Gemini"

CE CODE N'EXISTE PAS DANS BRAINSTORMING NORMAL!

📋 PLAN DE REMÉDIATION COMPLET
CORRECTION CRITIQUE #1: Forcer l'alternance dans BRAINSTORMING
Fichier: orchestration_v7.py:636-641

# AVANT (CASSÉ):
next_agent = message.get("next_agent", self.active_agent)
if next_agent != self.active_agent:
    self.active_agent = next_agent
    self.stagnation_detector.reset()

# APRÈS (CORRIGÉ):
# V7 FIX: FORCE alternance Gemini↔Claude (comme EVOLUTION_BRAINSTORM)
previous_agent = self.active_agent
self.active_agent = "Claude" if self.active_agent == "Gemini" else "Gemini"
self.stagnation_detector.reset()
print(f"[BRAINSTORM] {previous_agent} → {self.active_agent}", file=sys.stderr)

CORRECTION CRITIQUE #2: Claude driver - next_agent par défaut
Fichier: claude_driver_hybrid.py:298-301

# AVANT (CASSÉ):
next_agent = "Claude"
if "gemini" in content.lower() and action_type == "TALK":
    next_agent = "Gemini"

# APRÈS (CORRIGÉ):
# V7 FIX: Alterner par défaut (l'orchestrateur force aussi maintenant)
next_agent = "Gemini"  # Default: passer à Gemini

CORRECTION #3: Protocol validateur - ne pas garder sender
Fichier: protocol_v7.py:71-79

# AVANT (CASSÉ):
@field_validator('next_agent', mode='before')
@classmethod
def default_next_agent(cls, v: Optional[str], info: ValidationInfo) -> Optional[str]:
    """Si next_agent oublié, reste sur même agent"""
    if v is None and info.data.get('sender'):
        return info.data['sender']  # ⚠️ GARDE LE MÊME
    ...

# APRÈS (CORRIGÉ):
@field_validator('next_agent', mode='before')
@classmethod
def default_next_agent(cls, v: Optional[str], info: ValidationInfo) -> Optional[str]:
    """Si next_agent oublié, ALTERNER vers l'autre agent"""
    if v is None and info.data.get('sender'):
        sender = info.data['sender'].capitalize()
        # V7 FIX: Alterner au lieu de rester sur le même
        return "Claude" if sender == "Gemini" else "Gemini"
    ...

CORRECTION #4: Améliorer feedback SWARM
Fichier: orchestration_v7.py:502-515

# V7 FIX: Feedback visuel amélioré
for agent_data in agent_outputs:
    agent_id = agent_data.get("agent_id", "")
    content = agent_data.get("content", "")
    status = agent_data.get("status", "success")
    agent_name = "🤖 Gemini" if "gemini" in agent_id.lower() else "🧠 Claude"
    
    if status == "error" or content.startswith("Error:") or not content.strip():
        error_msg = agent_data.get("error") or content or "[No response]"
        formatted_output += f"\n{agent_name} ❌ ERREUR:\n{error_msg}\n{'─'*40}\n"
    else:
        formatted_output += f"\n{agent_name}:\n{content}\n{'─'*40}\n"

CORRECTION #5 (OPTIONNELLE): Désactiver SWARM auto-route
Pour diagnostiquer, ajoutez dans .env:

SWARM_AUTO_ROUTE=False

Cela forcera l'utilisation du mode BRAINSTORMING classique (avec les corrections #1-3).

📊 RÉSUMÉ DES CORRECTIONS
Priorité	Fichier	Correction	Impact
P0	orchestration_v7.py:636	Forcer alternance	Restaure échange Gemini↔Claude
P0	claude_driver_hybrid.py:298	next_agent = "Gemini"	Claude délègue à Gemini
P1	protocol_v7.py:74	Alterner au lieu de garder sender	Support alternnance
P2	orchestration_v7.py:502	Feedback visuel SWARM	Voir erreurs Claude
P3	.env	SWARM_AUTO_ROUTE=False	Diagnostic
Driver, le diagnostic est COMPLET.

Le problème fondamental est triple:

SWARM activé par défaut → Bypass le BRAINSTORMING
BRAINSTORMING ne force pas l'alternance (contrairement à EVOLUTION_BRAINSTORM)
Claude driver garde Claude → Pas de passage à Gemini
Voulez-vous que j'applique les corrections P0 immédiatement ?