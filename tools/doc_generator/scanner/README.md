# scanner

Repository scanning and file discovery.

Classes:
    RepoScanner: Scans repository for all folders and files
    FileClassifier: Classifies files by type (Python, TypeScript, etc.)
    ChangeDetector: Detects changes since last commit

## Overview

| Metric | Value |
|--------|-------|
| **Path** | `C:\Code\NEXUS\NEXUS-N7A\tools\doc_generator\scanner` |
| **Modules** | 4 |
| **Total Lines** | 608 |
| **Classes** | 6 |
| **Functions** | 0 |

## Architecture

```mermaid
classDiagram
    class ChangeInfo {
        +Path path
        +str change_type
        +Optional[Path] old_path
        +int lines_added
        +int lines_deleted
    }
    class ChangeReport {
        +str since
        +str until
        +datetime timestamp
        +list[ChangeInfo] changes
        +list[Path] new_folders
        +list[Path] deleted_folders
        +total_changes(self) int
        +files_added(self) int
        +files_modified(self) int
        +files_deleted(self) int
    }
    class ChangeDetector {
        +STATE_FILE
        +config
        +repo_root
        +state_file
        -__init__(self, config: DocGeneratorConfig)
        +detect_changes(self, since: str=...) ChangeReport
        -_parse_status(self, status: str) str
        -_detect_new_folders(self, since: str) list[Path]
        -_detect_deleted_folders(self, since: str) list[Path]
        -_get_current_commit(self) str
        +save_state(self) None
        +load_state(self) Optional[dict]
        +detect_since_last_run(self) ChangeReport
    }
    class FileType {
        +PYTHON_MODULE
        +PYTHON_INIT
        +PYTHON_TEST
        +PYTHON_CONFIG
        +TYPESCRIPT_COMPONENT
        +TYPESCRIPT_HOOK
        +TYPESCRIPT_UTIL
        +TYPESCRIPT_TYPE
        +MARKDOWN_README
        +MARKDOWN_DOC
        +CONFIG_JSON
        +CONFIG_YAML
        +CONFIG_TOML
        +CONFIG_ENV
        +UNKNOWN
    }
    Enum <|-- FileType
    class FileClassifier {
        +config
        -__init__(self, config: DocGeneratorConfig)
        +classify(self, file_path: Path) FileType
        -_classify_python(self, file_path: Path) FileType
        -_classify_typescript(self, file_path: Path) FileType
        -_classify_markdown(self, file_path: Path) FileType
        +get_file_purpose(self, file_path: Path) Optional[str]
        +is_test_file(self, file_path: Path) bool
        +is_documentation(self, file_path: Path) bool
    }
    class RepoScanner {
        +config
        +repo_root
        -__init__(self, config: DocGeneratorConfig)
        +scan(self) list[FolderInfo]
        -_walk_directories(self) Iterator[Path]
        -_analyze_folder(self, folder_path: Path) FolderInfo
        +detect_new_folders(self, since: str=...) list[Path]
        +get_changed_files(self, since: str=...) list[Path]
        +get_all_python_files(self) list[Path]
        +get_statistics(self) dict
    }
```

## Modules

| Module | Description | Classes | Functions |
|--------|-------------|---------|-----------|
| [change_detector](change_detector.py) | Detects changes in the repository since last run or commit. | 3 | 0 |
| [file_classifier](file_classifier.py) | Classifies files by type and purpose. | 2 | 0 |
| [repo_scanner](repo_scanner.py) | Scans repository for all folders and files with auto-discovery. | 1 | 0 |





---
*Auto-generated by nexus-doc-generator 1.0.0 - 2025-12-16 19:13*